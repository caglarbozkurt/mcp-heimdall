node_modules/
dist/
*.tgz
.DS_Store
.tmp/

# environment / secrets — never commit these
.env
.env.*
!.env.example

# built browser bundle for GitHub Pages (rebuilt by CI / build:pages)
pages/heimdall.js

# private Obsidian vault dev-log — not for the public repo
index.md
